# Dependencies for Vercel's Python runtime (mirrors pyproject.toml, minus the
# local yunoball-db package which the API runtime does not import).
fastapi>=0.115
pydantic>=2.10
pydantic-settings>=2.7
sqlalchemy>=2.0
psycopg[binary]>=3.2
redis>=5.2
openai>=1.59
sqlglot>=26.0
anyio>=4.0
